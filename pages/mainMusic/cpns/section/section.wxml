<view class="section">
  <view class="header">
    <view class="title">{{title}}</view>
    <view class="moreBtn" wx:if="{{isShowMoreBtn}}" bind:tap="onMoreBtnClick">
      <text>更多</text>
      <van-icon name="arrow" />
    </view>
  </view>
  <slot></slot>
</view>